#!/usr/bin/env sh
DEST=$1
SRC="{{ galaxify_target_dir }}"

mkdir -p $DEST
for FILE in .gitignore README.md LICENSE test.yml .travis.yml requirements.txt dcb-os.yml
do
    cp $SRC/$FILE $DEST/$FILE
done

for MAIN in meta tasks
do
    mkdir -p $DEST/$MAIN
    cp $SRC/${MAIN}-main.yml $DEST/$MAIN/main.yml
done
